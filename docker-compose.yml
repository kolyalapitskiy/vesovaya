services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vesovaya
      POSTGRES_USER: vesovaya
      POSTGRES_PASSWORD: vesovaya
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
